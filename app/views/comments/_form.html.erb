<%= form_with(model: [post, post.comments.build], class: "mt-3") do |form| %>
  <div class="form-control">
    <%= form.rich_text_area :body  %>
    <%= form.submit submit_label, disable_with: 'Replying...', class: 'btn btn-primary mt-1' %>
  </div>
<% end %>