<div id="<%= dom_id post %>">
  <h2> <%= post.title %> </h2>
  <h6>Views: <%= pluralize(post.views, 'view') %> </h6>
  <h5>Post by: <%= link_to post.user.name, user_path(post.user) %> </h5>

  <div>
    <%= post.body %>
  </div>

</div>
